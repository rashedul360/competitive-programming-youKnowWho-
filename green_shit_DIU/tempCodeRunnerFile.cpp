   for (int i = 1; i <= n; i++)
     {
          if (i % 2 == 0)
          {
               if (i == m)
               {
                    cout << i << endl;
                    break;
               }
          }
          count++;
     }